<%= form_with(model: property_label, local: true) do |form| %>
  <% if property_label.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(property_label.errors.count, "error") %> prohibited this property_label from being saved:</h2>

      <ul>
      <% property_label.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
